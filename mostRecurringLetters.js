/**
 * Return the character that repeats the most in a string.
 *
 * @example
 *  let characters = mostRecurringLetters('hello world');
 *  assert(characters).toBe(['l']);
 *
 *  characters = mostRecurringLetters('world');
 *  assert(characters).toBe(['w','o','r','l','d']);
 *
 * @param {String} str    A word, sentence, etc.
 * @returns {String[]}      The characters
 */
function mostRecurringLetters(str) {}

module.exports = mostRecurringLetters;
